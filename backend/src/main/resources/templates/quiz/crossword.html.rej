diff a/src/main/resources/templates/quiz/crossword.html b/src/main/resources/templates/quiz/crossword.html	(rejected hunks)
@@ -37,195 +37,251 @@
 </div>
 
 <script>
-window.onload = async function () {
-    if (!window.userId) {
-        alert('로그인 후 이용해주세요.');
-        location.href = '/login';
-        return;
-    }
+    window.onload = async function () {
+        if (!window.userId) {
+            alert('로그인 후 이용해주세요.');
+            location.href = '/login';
+            return;
+        }
+
+        const response = await fetch(`/api/quiz/crossword?userId=${window.userId}`);
+        const quiz = await response.json();
+
+        const width = 21, height = 21;
+        const board = Array.from({length: height}, () => Array(width).fill(null));
+        const numberMap = Array.from({length: height}, () => Array(width).fill(null));
 
-    const response = await fetch(`/api/quiz/crossword?userId=${window.userId}`);
-    const quiz = await response.json();
-
-    const width = 21, height = 21;
-    const board = Array.from({length: height}, () => Array(width).fill(null));
-    const numberMap = Array.from({length: height}, () => Array(width).fill(null));
-
-    quiz.items.forEach(item => {
-        let r = item.row, c = item.col;
-        for (let i = 0; i < item.length; i++) {
-            if (item.direction === "across") {
-                board[r][c+i] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item) };
-                if (i === 0) numberMap[r][c+i] = item.number;
-            } else {
-                board[r+i][c] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item) };
-                if (i === 0) numberMap[r+i][c] = item.number;
+        quiz.items.forEach(item => {
+            let r = item.row, c = item.col;
+            for (let i = 0; i < item.length; i++) {
+                if (item.direction === "across") {
+                    board[r][c+i] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item) };
+                    if (i === 0) numberMap[r][c+i] = item.number;
+                } else {
+                    board[r+i][c] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item) };
+                    if (i === 0) numberMap[r+i][c] = item.number;
+                }
             }
-        }
-    });
-
-    const boardDiv = document.getElementById("crosswordBoard");
-    const table = document.createElement("table");
-    table.style.borderCollapse = "collapse";
-    table.style.margin = "0 auto";
-    table.style.background = "#fffafc";
-    table.style.boxShadow = "0 4px 16px rgba(0,0,0,0.08)";
-    table.style.borderRadius = "20px";
-    
-    // 번호 매핑을 위한 2D 배열 (가로, 세로 각각)
-    const acrossNumbers = Array.from({length: height}, () => Array(width).fill(null));
-    const downNumbers = Array.from({length: height}, () => Array(width).fill(null));
-    
-    quiz.items.forEach(item => {
-        let r = item.row, c = item.col;
-        for (let i = 0; i < item.length; i++) {
-            if (item.direction === "across") {
-                board[r][c+i] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item), direction: "across" };
-                if (i === 0) acrossNumbers[r][c+i] = item.number;
-            } else {
-                board[r+i][c] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item), direction: "down" };
-                if (i === 0) downNumbers[r+i][c] = item.number;
+        });
+
+        const boardDiv = document.getElementById("crosswordBoard");
+        const table = document.createElement("table");
+        table.style.borderCollapse = "collapse";
+        table.style.margin = "0 auto";
+        table.style.background = "#fffafc";
+        table.style.boxShadow = "0 4px 16px rgba(0,0,0,0.08)";
+        table.style.borderRadius = "20px";
+
+        // 번호 매핑을 위한 2D 배열 (가로, 세로 각각)
+        const acrossNumbers = Array.from({length: height}, () => Array(width).fill(null));
+        const downNumbers = Array.from({length: height}, () => Array(width).fill(null));
+
+        quiz.items.forEach(item => {
+            let r = item.row, c = item.col;
+            for (let i = 0; i < item.length; i++) {
+                if (item.direction === "across") {
+                    board[r][c+i] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item), direction: "across" };
+                    if (i === 0) acrossNumbers[r][c+i] = item.number;
+                } else {
+                    board[r+i][c] = { term: item.term, idx: i, number: i === 0 ? item.number : null, itemIdx: quiz.items.indexOf(item), direction: "down" };
+                    if (i === 0) downNumbers[r+i][c] = item.number;
+                }
             }
-        }
-    });
-    
-    for (let r = 0; r < height; r++) {
-        const tr = document.createElement("tr");
-        for (let c = 0; c < width; c++) {
-            const td = document.createElement("td");
-            td.style.width = td.style.height = "36px";
-            td.style.border = "1.5px solid #d0d0d0";
-            td.style.background = board[r][c] ? "#fff" : "#f0f0f0";
-            td.style.position = "relative";
-            td.style.borderRadius = "8px";
-            
-            if (board[r][c]) {
-                td.className = "crossword-cell";
-                td.dataset.row = r;
-                td.dataset.col = c;
-
-                // 번호 표시 (가로/세로 모두 표시)
-                if (acrossNumbers[r][c] || downNumbers[r][c]) {
-                    const numberDiv = document.createElement("div");
-                    numberDiv.style.position = "absolute";
-                    numberDiv.style.top = "2px";
-                    numberDiv.style.left = "4px";
-                    numberDiv.style.fontSize = "8px";
-                    numberDiv.style.lineHeight = "1";
-                    
-                    if (acrossNumbers[r][c] && downNumbers[r][c]) {
-                        // 겹치는 경우
-                        numberDiv.innerHTML = `<span style="color:#4a90e2;">${acrossNumbers[r][c]}</span><br><span style="color:#ff69b4;">${downNumbers[r][c]}</span>`;
-                    } else if (acrossNumbers[r][c]) {
-                        // 가로만
-                        numberDiv.innerHTML = `<span style="color:#4a90e2;">${acrossNumbers[r][c]}</span>`;
-                    } else {
-                        // 세로만
-                        numberDiv.innerHTML = `<span style="color:#ff69b4;">${downNumbers[r][c]}</span>`;
+        });
+
+        for (let r = 0; r < height; r++) {
+            const tr = document.createElement("tr");
+            for (let c = 0; c < width; c++) {
+                const td = document.createElement("td");
+                td.style.width = td.style.height = "36px";
+                td.style.border = "1.5px solid #d0d0d0";
+                td.style.background = board[r][c] ? "#fff" : "#f0f0f0";
+                td.style.position = "relative";
+                td.style.borderRadius = "8px";
+
+                if (board[r][c]) {
+                    td.className = "crossword-cell";
+                    td.dataset.row = r;
+                    td.dataset.col = c;
+
+                    // 번호 표시 (가로/세로 모두 표시)
+                    if (acrossNumbers[r][c] || downNumbers[r][c]) {
+                        const numberDiv = document.createElement("div");
+                        numberDiv.style.position = "absolute";
+                        numberDiv.style.top = "2px";
+                        numberDiv.style.left = "4px";
+                        numberDiv.style.fontSize = "8px";
+                        numberDiv.style.lineHeight = "1";
+
+                        if (acrossNumbers[r][c] && downNumbers[r][c]) {
+                            // 겹치는 경우
+                            numberDiv.innerHTML = `<span style="color:#4a90e2;">${acrossNumbers[r][c]}</span><br><span style="color:#ff69b4;">${downNumbers[r][c]}</span>`;
+                        } else if (acrossNumbers[r][c]) {
+                            // 가로만
+                            numberDiv.innerHTML = `<span style="color:#4a90e2;">${acrossNumbers[r][c]}</span>`;
+                        } else {
+                            // 세로만
+                            numberDiv.innerHTML = `<span style="color:#ff69b4;">${downNumbers[r][c]}</span>`;
+                        }
+
+                        td.appendChild(numberDiv);
                     }
-                    td.appendChild(numberDiv);
+
+                    const input = document.createElement("input");
+                    input.type = "text";
+                    input.maxLength = 1;
+                    input.style.width = "100%";
+                    input.style.height = "100%";
+                    input.style.textAlign = "center";
+                    input.style.fontSize = "18px";
+                    input.style.border = "none";
+                    input.style.background = "transparent";
+                    input.style.caretColor = board[r][c].direction === "across" ? "#4a90e2" : "#ff69b4";
+                    input.style.borderRadius = "8px";
+                    input.dataset.row = r;
+                    input.dataset.col = c;
+                    input.readOnly = true;
+                    td.appendChild(input);
                 }
 
-                const input = document.createElement("input");
-                input.type = "text";
-                input.maxLength = 1;
-                input.style.width = "100%";
-                input.style.height = "100%";
-                input.style.textAlign = "center";
-                input.style.fontSize = "18px";
-                input.style.border = "none";
-                input.style.background = "transparent";
-                input.style.caretColor = board[r][c].direction === "across" ? "#4a90e2" : "#ff69b4";
-                input.style.borderRadius = "8px";
-                input.dataset.row = r;
-                input.dataset.col = c;
-                input.readOnly = true;
-                td.appendChild(input);
+                tr.appendChild(td);
             }
-            tr.appendChild(td);
-        }
-        table.appendChild(tr);
-    }
-    boardDiv.appendChild(table);
-
-    const clueSection = document.getElementById("clueSection");
-    clueSection.innerHTML = quiz.items.map((item, idx) => `
-        <div class="crossword-quiz-row" id="quizRow_${idx}" style="display: flex; align-items: center; margin-bottom: 16px; background: ${item.direction === 'across' ? '#e6f3ff' : '#fff0fa'}; border-radius: 16px; padding: 12px 16px; box-shadow: 0 2px 6px rgba(0,0,0,0.05);">
-            <span style="font-weight:bold; color:${item.direction === 'across' ? '#4a90e2' : '#ff69b4'}; min-width:60px;">${item.number}. ${item.direction === 'across' ? '가로' : '세로'}</span>
-            <span style="margin-left:10px; flex:1; font-size:14px; color:#333;">${(item.clue || item.question).replace(/\n/g, '<br>')}</span>
-            <input type="text" id="answerInput_${idx}" maxlength="${item.length}" style="font-size:16px; width:90px; border-radius: 8px; border: 1px solid #ccc; padding: 4px 6px;">
-            <button id="submitBtn_${idx}" style="margin-left:6px; border:none; background:${item.direction === 'across' ? '#4a90e2' : '#ff69b4'}; color:white; border-radius:8px; padding:5px 8px;">제출</button>
-            <button id="hintBtn_${idx}" style="margin-left:6px; border:none; background:${item.direction === 'across' ? '#b3d9ff' : '#ffccdc'}; color:#333; border-radius:8px; padding:5px 8px;">초성 힌트</button>
-            <span id="hintText_${idx}" style="display:none; margin-left:10px; color:#888;">${item.initialHint}</span>
-            <span id="resultIcon_${idx}" style="margin-left:12px; font-size:22px;"></span>
-        </div>
-    `).join("");
-
-    const cellToItems = {};
-    quiz.items.forEach((item, idx) => {
-        let r = item.row, c = item.col;
-        for (let i = 0; i < item.length; i++) {
-            let key = item.direction === "across" ? `${r},${c+i}` : `${r+i},${c}`;
-            if (!cellToItems[key]) cellToItems[key] = [];
-            cellToItems[key].push({itemIdx: idx, char: item.term[i]});
+            table.appendChild(tr);
         }
-    });
+        boardDiv.appendChild(table);
 
-    const correctMap = Array(quiz.items.length).fill(false);
-    quiz.items.forEach((item, idx) => {
-        document.getElementById(`submitBtn_${idx}`).onclick = function() {
-            const val = document.getElementById(`answerInput_${idx}`).value.trim();
-            const quizRow = document.getElementById(`quizRow_${idx}`);
-            const icon = document.getElementById(`resultIcon_${idx}`);
-            let isCorrect = (val === item.term);
-            correctMap[idx] = isCorrect;
+        const clueSection = document.getElementById("clueSection");
+        clueSection.innerHTML = quiz.items.map((item, idx) => {
+            // clueText에서 정답을 □로 치환
+            const clueText = (item.clue || item.question)
+                .replace(new RegExp(item.term, "g"), "□".repeat(item.term.length))
+                .replace(/\n/g, "<br>");
+            return `
+            <div class="crossword-quiz-row" id="quizRow_${idx}"
+                style="display: flex; align-items: center; margin-bottom: 16px; background: ${item.direction === 'across' ? '#e6f3ff' : '#fff0fa'};
+                       border-radius: 16px; padding: 12px 16px; box-shadow: 0 2px 6px rgba(0,0,0,0.05);">
+                <span style="font-weight:bold; color:${item.direction === 'across' ? '#4a90e2' : '#ff69b4'}; min-width:60px;">
+                    ${item.number}. ${item.direction === 'across' ? '가로' : '세로'}
+                </span>
+                <span style="margin-left:10px; flex:1; font-size:14px; color:#333;"> ${clueText} </span>
+                <input type="text" id="answerInput_${idx}" maxlength="${item.length}"
+                       style="font-size:16px; width:90px; border-radius: 8px; border: 1px solid #ccc; padding: 4px 6px;">
+                <button id="submitBtn_${idx}" style="margin-left:6px; border:none; background:${item.direction === 'across' ? '#4a90e2' : '#ff69b4'};
+                            color:white; border-radius:8px; padding:5px 8px;"> 제출 </button>
+                <button id="hintBtn_${idx}" style="margin-left:6px; border:none; background:${item.direction === 'across' ? '#b3d9ff' : '#ffccdc'};
+                            color:#333; border-radius:8px; padding:5px 8px;"> 초성 힌트 </button>
+                <span id="hintText_${idx}" style="display:none; margin-left:10px; color:#888;"> ${item.initialHint} </span>
+                <span id="resultIcon_${idx}" style="margin-left:12px; font-size:22px;"></span>
+            </div>
+            `;
+        }).join("");
 
+        const cellToItems = {};
+        quiz.items.forEach((item, idx) => {
             let r = item.row, c = item.col;
             for (let i = 0; i < item.length; i++) {
-                let rr = item.direction === "across" ? r : r+i;
-                let cc = item.direction === "across" ? c+i : c;
-                const input = table.rows[rr].cells[cc].querySelector("input");
-                const key = `${rr},${cc}`;
-                const isCross = cellToItems[key].length > 1;
-                const hasCorrect = cellToItems[key].some(ci => correctMap[ci.itemIdx]);
-
-                if (input) {
-                    if (isCorrect) {
-                        input.value = item.term[i];
-                        input.style.background = item.direction === "across" ? "#e6f3ff" : "#fff0fa";
-                        input.style.color = item.direction === "across" ? "#4a90e2" : "#ff69b4";
-                        input.style.fontWeight = "bold";
-                    } else {
-                        if (hasCorrect) {
-                            let correctChar = cellToItems[key].find(ci => correctMap[ci.itemIdx]).char;
-                            input.value = correctChar;
-                            // 겹치는 셀의 경우 방향에 따라 색상 결정
-                            let cellDirection = board[rr][cc].direction;
-                            input.style.background = cellDirection === "across" ? "#e6f3ff" : "#fff0fa";
-                            input.style.color = cellDirection === "across" ? "#4a90e2" : "#ff69b4";
+                let key = item.direction === "across" ? `${r},${c+i}` : `${r+i},${c}`;
+                if (!cellToItems[key]) cellToItems[key] = [];
+                cellToItems[key].push({itemIdx: idx, char: item.term[i]});
+            }
+        });
+
+        const correctMap = Array(quiz.items.length).fill(false);
+
+        quiz.items.forEach((item, idx) => {
+            document.getElementById(`submitBtn_${idx}`).onclick = function() {
+                const val = document.getElementById(`answerInput_${idx}`).value.trim();
+                const quizRow = document.getElementById(`quizRow_${idx}`);
+                const icon = document.getElementById(`resultIcon_${idx}`);
+                let isCorrect = (val === item.term);
+                correctMap[idx] = isCorrect;
+
+                let r = item.row, c = item.col;
+                for (let i = 0; i < item.length; i++) {
+                    let rr = item.direction === "across" ? r : r+i;
+                    let cc = item.direction === "across" ? c+i : c;
+                    const input = table.rows[rr].cells[cc].querySelector("input");
+                    const key = `${rr},${cc}`;
+                    const isCross = cellToItems[key].length > 1;
+                    const hasCorrect = cellToItems[key].some(ci => correctMap[ci.itemIdx]);
+
+                    if (input) {
+                        if (isCorrect) {
+                            // ✅ 정답일 때
+                            input.value = item.term[i];
+                            input.style.background = item.direction === "across" ? "#f2f8ff" : "#fff6fb";
+                            input.style.color = "#000000";
+                            input.style.fontWeight = "bold";
                         } else {
-                            input.value = '';
-                            input.style.background = "#ffe0e0";
-                            input.style.color = "#ff4d4f";
+                            // ❌ 오답일 때
+                            if (hasCorrect) {
+                                let correctChar = cellToItems[key].find(ci => correctMap[ci.itemIdx]).char;
+                                input.value = correctChar;
+                                let cellDirection = board[rr][cc].direction;
+                                input.style.background = cellDirection === "across" ? "#e6f3ff" : "#fff0fa";
+                                input.style.color = cellDirection === "across" ? "#4a90e2" : "#ff69b4";
+                            } else {
+                                input.value = '';
+                                input.style.background = "#ffe0e0";
+                                input.style.color = "#ff4d4f";
+                            }
+                            input.style.fontWeight = "bold";
                         }
-                        input.style.fontWeight = "bold";
+                        input.disabled = true;
                     }
-                    input.disabled = true;
                 }
-            }
 
-            quizRow.style.background = isCorrect ? (item.direction === "across" ? "#e6f3ff" : "#fff0fa") : "#ffe0e0";
-            quizRow.style.border = isCorrect ? (item.direction === "across" ? "2px solid #4a90e2" : "2px solid #ff69b4") : "2px solid #ff4d4f";
-            icon.innerHTML = isCorrect ? `<span style="color:${item.direction === "across" ? "#4a90e2" : "#ff69b4"};">✔</span>` : '<span style="color:#ff4d4f;">✖</span>';
-            document.getElementById(`answerInput_${idx}`).disabled = true;
-            document.getElementById(`submitBtn_${idx}`).disabled = true;
-        };
-
-        document.getElementById(`hintBtn_${idx}`).onclick = function() {
-            document.getElementById(`hintText_${idx}`).style.display = "inline";
-        };
-    });
+                quizRow.style.background = isCorrect ? (item.direction === "across" ? "#e6f3ff" : "#fff0fa") : "#ffe0e0";
+                quizRow.style.border = isCorrect ? (item.direction === "across" ? "2px solid #4a90e2" : "2px solid #ff69b4") : "2px solid #ff4d4f";
+                icon.innerHTML = isCorrect ? `<span style="color:${item.direction === "across" ? "#4a90e2" : "#ff69b4"};">✔</span>` : '<span style="color:#ff4d4f;">✖</span>';
+
+                if (isCorrect) {
+                    // ✅ 정답이면 입력/버튼 막기
+                    document.getElementById(`answerInput_${idx}`).disabled = true;
+                    document.getElementById(`submitBtn_${idx}`).disabled = true;
+                } else {
+                    // ❌ 오답이면 버튼을 "정답" 버튼으로 전환
+                    const btn = document.getElementById(`submitBtn_${idx}`);
+                    btn.disabled = false;
+                    btn.textContent = "정답";
+                    btn.style.background = "#ff4d4f";
+                    btn.onclick = function() {
+    const inputBox = document.getElementById(`answerInput_${idx}`);
+    inputBox.value = "";
+    inputBox.style.display = "none";
+    const answerSpan = document.createElement("span");
+    answerSpan.textContent = item.term;
+    answerSpan.style.color = "#ff0000"; // 리스트에서도 빨강 표시
+    answerSpan.style.fontWeight = "bold";
+    answerSpan.style.marginLeft = "10px";
+    inputBox.parentNode.insertBefore(answerSpan, btn);
+    btn.disabled = true;
+
+    // === 보드판 셀 색상만 업데이트 (테두리 제거) ===
+    let r = item.row, c = item.col;
+    for (let i = 0; i < item.length; i++) {
+        let rr = item.direction === "across" ? r : r+i;
+        let cc = item.direction === "across" ? c+i : c;
+        const cell = table.rows[rr].cells[cc];
+        const input = cell.querySelector("input");
+        if (input) {
+            input.value = item.term[i];
+            input.style.background = "#ffe0e0"; // 연한 빨강 배경
+            input.style.color = "#ff0000";      // 글자만 빨강
+            input.style.fontWeight = "bold";
+            input.style.border = "none";        // 테두리 완전히 제거
+        }
+    }
 };
+
+                }
+            };
+
+            document.getElementById(`hintBtn_${idx}`).onclick = function() {
+                document.getElementById(`hintText_${idx}`).style.display = "inline";
+            };
+        });
+    };
 </script>
 </body>
 </html>